% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{skilldown_config}
\alias{skilldown_config}
\title{Site configuration: \verb{_skilldown.yml}}
\description{
An optional \verb{_skilldown.yml} at the root of a skill collection overrides
the defaults \code{\link[=build_site]{build_site()}} uses. Without the file the defaults do all
the work; the file never has to exist. The keys mirror \verb{_pkgdown.yml}
where they transfer.\tabular{ll}{
   Key \tab Effect \cr
   \code{title} \tab Site title. Default: the name of the collection directory. \cr
   \code{url} \tab Site URL, written as Quarto's \code{site-url} so the sitemap and canonical links resolve there. \cr
   \code{theme} \tab Bootswatch theme of the HTML format. Default \code{cosmo}. \code{bootswatch} is accepted as an alias. \cr
   \code{news} \tab Root-relative path of the changelog page. Default: \code{NEWS.md}, then \code{CHANGELOG.md}. \cr
   \code{exclude} \tab Globs, relative to the collection root, dropped from discovery and rendering: a skill whose directory matches, an article whose path matches. \cr
   \code{navbar} \tab \code{left} and \code{right} lists of Quarto navbar entries. An entry whose \code{text} matches a generated entry replaces it; any other entry is appended. \cr
   \code{reference} \tab Sections of the skills index, each with a \code{title}, an optional \code{desc} and \code{contents} (skill names, or globs over names and directories). A skill no section selects is listed last under "Other skills", with a validation note. \cr
   \code{articles} \tab Sections of the articles index in the same shape; \code{contents} match \verb{docs/} files with or without the \verb{docs/} prefix and the \code{.md} extension. \cr
   \code{surfaces} \tab Reserved for the agents, commands and plugin-manifest sections of a later version. Read and ignored for now. \cr
}


Unknown keys are reported and ignored. A file that is not valid YAML, or
a key of the wrong shape, stops the build with a message naming it: the
configuration is the site author's own file, unlike the collection
content, which is only ever reported on.
}
\examples{
\dontrun{
# _skilldown.yml
# title: My skills
# url: https://example.org/skills
# theme: flatly
# exclude:
#   - skills/wip-*
# navbar:
#   right:
#     - text: Docs
#       href: https://example.org/docs
# reference:
#   - title: Core
#     desc: The skills to start with.
#     contents: [alpha, beta]
#   - title: Data
#     contents: [data-*]
}
}
